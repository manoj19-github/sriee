# Function Map — LangGraph Brain

Source: [LangGraph architecture](../../06-LANGGRAPH_ARCHITECTURE.md), [state design](../../07-STATE_DESIGN.md), and [agent blueprint](../../08-AGENT_BLUEPRINT.md).

Technology: Python, LangGraph, typed state, PostgreSQL checkpointer, provider-neutral model gateway.

| Global ID | Canonical name | Function | Description | Reads | Writes | Status | Notes |
|---|---|---|---|---|---|---|---|
| 120000 | `python-langgraph-build-graph-mandatory-p0-complete-current-v1` | `buildJarvisGraph` | Validates the exact versioned node registry, canonical state/reducers, compile settings and explicitly durable checkpointer binding, then compiles the fixed allow/ask/deny/revision topology.<br>Fails startup with safe codes when any contract is incompatible. | node registry; state schema/reducers; graph settings; checkpointer binding | memory: named compiled graph with checkpointer | complete/current/v1 | Construction never invokes a node, model or OS tool. PostgreSQL saver implementation remains tracked by 140004; verified by 23 focused tests and full regression. |
| 120001 | `python-langgraph-normalize-request-mandatory-p0-complete-current-v1` | `normalizeRequest` | Validates task input, actor/device metadata, v1 contract and workflow status; preserves valid IDs or assigns a task ID and task-derived thread ID, then returns a minimal planning-state delta.<br>Rejects malformed, oversized or incompatible input with safe codes. | graph task request and identity metadata; injected non-secret bounds/ID source | graph state: contract/task/thread/request/status delta | complete/current/v1 | Text/transcript content, Unicode, whitespace and line endings are preserved exactly; input is never mutated. Verified by 25 focused normalization tests. |
| 120002 | `python-langgraph-load-context-mandatory-p0-planned-current-v1` | `loadBoundedContext` | Loads only authorized project, capability, policy and memory references needed for the request. | db: project/memory/policy; desktop capability manifest | graph state: context references | planned/current/v1 | Artifacts remain referenced, not embedded in checkpoints. |
| 120003 | `python-langgraph-classify-intent-mandatory-p0-planned-current-v1` | `classifyIntent` | Produces typed intent, confidence, scope and ambiguity from request/context. | normalized request; bounded context; model | graph state: intent | planned/current/v1 | Low-confidence consequential intent routes to clarification. |
| 120004 | `python-langgraph-create-plan-mandatory-p0-planned-current-v1` | `createPlanDraft` | Produces minimal dependency-aware typed actions and success criteria using registered capabilities only. | intent; context; capability manifest; model | graph state/db: plan draft | planned/current/v1 | Model never returns executable prose. |
| 120005 | `python-langgraph-validate-plan-mandatory-p0-planned-current-v1` | `validatePlan` | Validates schemas, dependency DAG, limits, known resources, action uniqueness and verification definitions. | plan draft; schemas; budgets | graph state: validated plan/errors | planned/current/v1 | Cycles, unknown capabilities and budget overflow fail closed. |
| 120006 | `python-langgraph-evaluate-plan-policy-mandatory-p0-planned-current-v1` | `evaluatePlanPolicy` | Obtains deterministic allow/ask/deny decision for every action and prevents risk splitting. | validated plan; policy snapshot | graph state/db: policy decisions | planned/current/v1 | Security model may recommend stricter policy, never weaker. |
| 120007 | `python-langgraph-pause-for-approval-mandatory-p0-planned-current-v1` | `pauseForApproval` | Persists JSON-safe action preview and invokes a durable interrupt for one exact action digest. | ask decision; action; actor/device | checkpoint/db: pending approval; event | planned/current/v1 | Side effects occur in a later node; node is replay-safe. |
| 120008 | `python-langgraph-resume-approval-mandatory-p0-planned-current-v1` | `resumeApproval` | Validates resume payload against pending interrupt and routes approved, denied or expired outcome. | checkpoint; approval decision | graph state: approval result | planned/current/v1 | Same thread ID required; one-time decision. |
| 120009 | `python-langgraph-dispatch-action-mandatory-p0-planned-current-v1` | `dispatchAction` | Emits one contract-valid action after dependencies, policy and approval are satisfied. | validated action; decisions; prior results | outbox/ws: action request; db: action state | planned/current/v1 | Side-effect concurrency is resource-bounded. |
| 120010 | `python-langgraph-collect-action-result-mandatory-p0-planned-current-v1` | `collectActionResult` | Correlates executor result/receipt, rejects mismatches and appends immutable result state. | ws/api: action result; pending action | graph/db: action result/event | planned/current/v1 | Duplicate receipt is reconciled, not re-applied. |
| 120011 | `python-langgraph-verify-outcome-mandatory-p0-planned-current-v1` | `verifyOutcome` | Runs independent read-only postconditions and classifies succeeded, partial, failed or uncertain outcome. | action results; observations; success criteria | graph state/db: verification result | planned/current/v1 | Dispatch success is not task success. |
| 120012 | `python-langgraph-revise-plan-optional-p1-planned-current-v1` | `revisePlan` | Creates bounded immutable plan revision after recoverable failure or corrected scope. | prior plan/results; revision budget; model | db/graph: new plan revision | planned/current/v1 | Maximum two automatic revisions; mutations need fresh policy. |
| 120013 | `python-langgraph-render-response-mandatory-p0-planned-current-v1` | `renderFinalResponse` | Produces concise user response grounded only in stored evidence and unresolved issues. | verified task state; model/templating | graph/db/api: final response | planned/current/v1 | No chain-of-thought or fabricated completion. |
| 120014 | `python-langgraph-call-local-model-mandatory-p0-complete-current-v1` | `callLocalModel` | Sends bounded system/user/assistant messages to an allowlisted loopback Ollama model and returns typed response metadata/content.<br>Supports JSON-schema output, bounded time/tokens/context, lifecycle health and sanitized failures. | local provider settings; typed messages; optional response schema | local Ollama API; memory: bounded model response/health | complete/current/v1 | No cloud fallback, tool execution, raw prompt logging or arbitrary provider URL. Model output remains untrusted. Verified by 19 deterministic tests and a live `qwen3:4b-instruct` structured-output smoke test. |

## Change rule

Update this map before changing nodes, edges, reducers, interrupt order, checkpoint content, retry/revision limits, prompts, or terminal-state logic.

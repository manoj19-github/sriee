# Function Map — LangGraph Brain

Source: [LangGraph architecture](../../06-LANGGRAPH_ARCHITECTURE.md), [state design](../../07-STATE_DESIGN.md), and [agent blueprint](../../08-AGENT_BLUEPRINT.md).

Technology: Python, LangGraph, typed state, PostgreSQL checkpointer, provider-neutral model gateway.

| Global ID | Canonical name | Function | Description | Reads | Writes | Status | Notes |
|---|---|---|---|---|---|---|---|
| 120000 | `python-langgraph-build-graph-mandatory-p0-complete-current-v1` | `buildJarvisGraph` | Validates the exact versioned node registry, canonical state/reducers, compile settings and explicitly durable checkpointer binding, then compiles the fixed allow/ask/deny/revision topology.<br>Fails startup with safe codes when any contract is incompatible. | node registry; state schema/reducers; graph settings; checkpointer binding | memory: named compiled graph with checkpointer | complete/current/v1 | Construction never invokes a node, model or OS tool. PostgreSQL saver implementation remains tracked by 140004; verified by 23 focused tests and full regression. |
| 120001 | `python-langgraph-normalize-request-mandatory-p0-complete-current-v1` | `normalizeRequest` | Validates task input, actor/device metadata, v1 contract and workflow status; preserves valid IDs or assigns a task ID and task-derived thread ID, then returns a minimal planning-state delta.<br>Rejects malformed, oversized or incompatible input with safe codes. | graph task request and identity metadata; injected non-secret bounds/ID source | graph state: contract/task/thread/request/status delta | complete/current/v1 | Text/transcript content, Unicode, whitespace and line endings are preserved exactly; input is never mutated. Verified by 25 focused normalization tests. |
| 120002 | `python-langgraph-load-context-mandatory-p0-complete-current-v1` | `loadBoundedContext` | Concurrently loads versioned project, capability, policy and memory references under per-source deadlines and fixed limits; verifies actor/device ownership, kind/prefix and required security context.<br>Optional project/memory outages append safe retryable errors. | normalized request; injected authorized context sources | graph state: ordered opaque context reference IDs; optional safe errors | complete/current/v1 | Policy/capability are required and device-bound. Reference models cannot contain source content; concrete persistence/retrieval adapters remain separately tracked. Verified by 31 focused tests. |
| 120003 | `python-langgraph-classify-intent-mandatory-p0-complete-current-v1` | `classifyIntent` | Produces a schema-validated intent, confidence, authorized reference scope and fixed ambiguity codes from the normalized request and ephemeral bounded context summaries.<br>Malformed or contract-invalid model output receives one content-free repair attempt. | normalized request; authorized context references; injected summary resolver; local model gateway | graph state: typed intent projection | complete/current/v1 | Consequential low-confidence, unknown intent, missing required project scope, inconsistent target or unresolved ambiguity routes to clarification deterministically. Verified by 48 focused tests, full regression and a live local Qwen structured-output smoke test. |
| 120004 | `python-langgraph-create-plan-mandatory-p0-complete-current-v1` | `createPlanDraft` | Produces a bounded dependency-aware draft from strict model output, resolving only registered capability/version pairs, typed scalar bindings, opaque resources and declared verification codes.<br>Invalid model output receives one content-free repair attempt. | validated intent; authorized context references; injected planning context/capability manifest; local model gateway | graph state: checkpoint-safe typed plan draft | complete/current/v1 | Application derives stable semantic action/criterion IDs independent of model step labels. Raw paths, shell text, nested command blobs, model risk labels and executable prose cannot be represented. Verified by 65 focused tests, full regression and a live local Qwen structured-plan smoke test. |
| 120005 | `python-langgraph-validate-plan-mandatory-p0-complete-current-v1` | `validatePlan` | Revalidates a checkpointed plan against the v1 schema, current actor/device-bound capability manifest, known opaque resources, full dependency DAG and trusted aggregate budgets.<br>Requires unique action semantics and declared verification coverage for every action. | checkpointed plan draft; authorized context references; injected capability/resource resolver; validation budgets | graph state: canonical validated plan or typed safe failure | complete/current/v1 | Unknown capabilities/resources, invalid bindings, duplicate actions, missing verification, cycles and total/critical-path budget overflow fail closed. Verified by 15 focused tests and 348-test backend regression. |
| 120006 | `python-langgraph-evaluate-plan-policy-mandatory-p0-complete-current-v1` | `evaluatePlanPolicy` | Resolves an actor/device-bound deny-by-default snapshot and deterministically emits one stable allow/ask/deny decision for every validated action.<br>Aggregate combination rules elevate related/repeated actions together to prevent risk splitting; optional security analysis can only tighten decisions or risk. | validated plan; policy reference; injected policy snapshot resolver; optional bounded security assessment | graph state/checkpoint: minimal per-action policy decisions and executing/awaiting_approval/denied status | complete/current/v1 | R4 always denies, R3 cannot allow, R2 allow requires a scoped grant. Final desktop policy is re-evaluated before execution under 130003. Verified by 23 focused tests and 383-test backend regression. |
| 120007 | `python-langgraph-pause-for-approval-mandatory-p0-complete-current-v1` | `pauseForApproval` | Selects only the first plan-ordered ask action, derives a canonical actor/device/thread/policy-bound SHA-256 digest and persists one approval plus event through an idempotent atomic store before invoking LangGraph `interrupt()`.<br>The interrupt contains an exact bounded typed preview; resumed data is reduced to approval ID, digest and approve/deny only. | validated plan; ask decisions; task/thread/actor/device; injected atomic approval store | db: pending approval/event; durable interrupt payload; checkpoint: strict resume candidate | complete/current/v1 | Node restart replays the same deterministic persistence request and cannot duplicate approval/event. No side effect or blanket approval occurs. Verified by 16 focused tests—including real MemorySaver interrupt/resume—and 399-test backend regression. |
| 120008 | `python-langgraph-resume-approval-mandatory-p0-complete-current-v1` | `resumeApproval` | Revalidates the exact pending checkpoint and strict resume candidate, requires the runtime thread plus task/actor/device bindings to match, and atomically claims one authoritative approved, denied or expired resolution.<br>Only a bounded result projection remains in graph state. | checkpointed pending approval/resume candidate; runtime thread ID; injected atomic resolution store | graph state: typed approval result and executing/denied/expired status | complete/current/v1 | ID/digest mismatches fail before store access; concurrent replay permits one winner and never dispatches. Verified by 15 focused tests and 414-test backend regression. |
| 120009 | `python-langgraph-dispatch-action-mandatory-p0-complete-current-v1` | `dispatchAction` | Deterministically selects one unresolved dependency-ready action, revalidates complete policy/result coverage and exact ask-approval digest, then builds one bounded canonical executor request.<br>An injected atomic store reserves global/resource capacity while creating action state, event, lease and outbox exactly once. | validated plan; per-action decisions; prior results; optional consumed approval; runtime thread; trusted dispatch limits/store | db: queued action state/event/resource lease; outbox/ws: strict action request | complete/current/v1 | No executor is called by the graph node. Replay reuses one dispatch/event/outbox/lease; per-resource default is one in flight. Verified by 18 focused tests and 432-test backend regression. |
| 120010 | `python-langgraph-collect-action-result-mandatory-p0-complete-current-v1` | `collectActionResult` | Accepts only a bounded artifact-reference-only executor receipt, requires task/thread/action/dispatch/idempotency/device correlation with one queued action, and atomically persists or retrieves one immutable result/event while releasing its resource lease.<br>Late or timeout-overrun receipts become uncertain; the graph appends the exact minimal projection only when absent from checkpoint state. | ws/api: strict action result; executing graph state; injected atomic pending-action/result store; runtime thread | db: immutable action result/event and reconciled lease; graph: append-only minimal result and verifying status | complete/current/v1 | Same receipt/store replay recovers stale checkpoints without another durable write; an already-present exact checkpoint result emits an empty append delta. Conflicts fail closed. Verified by 30 focused tests and 462-test backend regression. |
| 120011 | `python-langgraph-verify-outcome-mandatory-p0-complete-current-v1` | `verifyOutcome` | Resolves capability/version-bound read-only probes, evaluates every declared success criterion independently of executor outcome, persists typed evidence, and classifies succeeded, partial, failed or uncertain deterministically.<br>Database and checkpoint replay reuse immutable verification identity without rerunning probes or duplicating observations. | validated plan criteria; correlated action-result receipts; prior verification observations; injected read-only probe registry/store | db: immutable verification record; graph: criterion/aggregate observations and deterministic status | complete/current/v1 | Dispatch success is never task success. Retryable failure/uncertainty may route to bounded revision; exhausted uncertainty remains explicit evidence and terminates safely as failed. Verified by 56 focused tests and 518-test backend regression. |
| 120012 | `python-langgraph-revise-plan-optional-p1-complete-current-v1` | `revisePlan` | Creates one immutable typed revision from current registered capabilities/resources after exact recoverable verification evidence or an identity-bound corrected-scope candidate.<br>Application-merges every executed action/criterion and append-only result correlation while permitting bounded corrective actions. | prior plan/results; verified criterion/aggregate evidence or corrected scope; current planning bundle; revision budget; local model | db: immutable plan revision; graph: replacement plan, incremented revision, cleared policy/approval and planning status | complete/current/v1 | Maximum two revisions. Changed mutation plans always return through validation and fresh policy/approval; failed/cancelled/uncertain receipts cannot unlock new dependencies. Verified by 40 focused tests, 558-test regression and live configured-model smoke test. |
| 120013 | `python-langgraph-render-response-mandatory-p0-complete-current-v1` | `renderFinalResponse` | Deterministically renders a bounded typed response from terminal status, stored verification/policy/approval evidence and opaque receipt/evidence references, then persists it immutably.<br>Checkpoint/store replay reuses stable response identity without duplicate writes. | terminal graph state; stored verification observations/results; policy/approval evidence; fixed templates | db: immutable final response; graph/API: typed final response | complete/current/v1 | No model prose, chain-of-thought, raw artifact content or fabricated completion can enter the response. Verified by 22 focused tests and 580-test regression. |
| 120014 | `python-langgraph-call-local-model-mandatory-p0-complete-current-v2` | `callLocalModel` | Routes bounded non-streaming chat to the explicitly allowlisted Qwen-compatible endpoint in development/test and to loopback Ollama only in production.<br>Supports optional secret Bearer authentication, JSON Schema output, lifecycle health and sanitized failures. | environment-bound provider settings; typed messages; optional response schema/secret credential | configured chat API; memory: bounded model response/health | complete/current/v2 | Production rejects remote URLs and credentials; development accepts only `qwen.msqube.in/api/chat` on its default HTTP/HTTPS port. No automatic fallback, tool execution or raw content logging. Revalidated 2026-07-04 with 31 focused tests, 580-test backend regression and a successful content-minimal authenticated live health/chat probe. |
| 120015 | `python-langgraph-propose-task-reflection-optional-p2-planned-current-v1` | `proposeTaskReflection` | Evaluates verified outcome evidence and proposes a bounded, reviewable lesson or prompt/test improvement candidate. | verified outcome; safe errors; user correction; current version references | db/UI: reflection candidate and evidence references | planned/current/v1 | Never edits prompts, policy, tools or memory automatically; failure is not converted into fabricated learning. |
| 120016 | `python-langgraph-coordinate-specialists-optional-p1-planned-current-v1` | `coordinateSpecialistWorkflow` | Runs a declared bounded planner/research/coder/reviewer/security/verifier sequence with typed handoffs, deadlines and depth limits. | approved workflow definition; scoped context; specialist contracts | graph: typed specialist results and provenance | planned/current/v1 | Maximum depth two; side effects remain in the standard action pipeline and model consensus is not treated as truth. |

## Change rule

Update this map before changing nodes, edges, reducers, interrupt order, checkpoint content, retry/revision limits, prompts, or terminal-state logic.

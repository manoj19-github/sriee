# Function Map — FastAPI Platform

Source: [FastAPI backend](../../23-FASTAPI_BACKEND.md), [API specification](../../26-API_SPECIFICATION.md), and [WebSocket protocol](../../27-WEBSOCKET_PROTOCOL.md).

Technology: Python, FastAPI, Pydantic, Uvicorn, PostgreSQL, optional Redis.

Naming: `{platform}-{component}-{function}-{mandatory|optional}-{priority}-{completion}-{lifecycle}-{version}`.

| Global ID | Canonical name | Function | Description | Reads | Writes | Status | Notes |
|---|---|---|---|---|---|---|---|
| 110000 | `python-fastapi-load-settings-mandatory-p0-complete-current-v1` | `loadSettings` | Loads typed immutable configuration from environment, optional dotenv and explicit overrides.<br>Validates the isolated `jarvis` schema, separate JWT secrets, loopback production binding and Redis authentication; exposes redacted diagnostics and a deterministic safe fingerprint. | env; optional dotenv; explicit overrides | memory: immutable settings; safe diagnostics/fingerprint | complete/current/v1 | Implemented in `backend/src/jarvis/config/settings.py`; no import-time settings instance; verified by 11 focused tests. |
| 110001 | `python-fastapi-manage-lifespan-mandatory-p0-complete-current-v1` | `manageApplicationLifespan` | Initializes and health-checks telemetry, database, checkpointer, graph, provider and worker resources in strict dependency order.<br>On failure it rolls back partial startup in reverse order; on shutdown it stops intake, bounds worker drain time and closes every resource even after individual cleanup errors. | immutable settings; ordered resource factories; dependency health | process: runtime state/readiness, managed resource lifecycle | complete/current/v1 | Implemented in `jarvis/runtime/lifecycle.py`; errors/readiness are secret-safe; verified by FastAPI integration and failure-path tests. |
| 110002 | `python-fastapi-authenticate-desktop-mandatory-p0-complete-current-v1` | `authenticateDesktopSession` | Validates a strictly signed, short-lived desktop JWT plus device, live session, session epoch, requested contract version and one-time request nonce.<br>Computes the server/token/device/session contract intersection and binds an immutable actor/device/session principal to request state. | authorization and binding headers; device registry; session registry; nonce store; supported contract range | request: immutable authenticated principal; nonce store: consumed request nonce | complete/current/v1 | HS256/issuer/audience/required claims are fixed; loopback provides no trust; mismatches return generic 401 and unsupported contracts return 426. |
| 110003 | `python-fastapi-create-task-mandatory-p0-complete-current-v1` | `createTask` | Validates an authenticated text/transcript request plus idempotency and correlation identifiers.<br>Atomically creates an immutable task, sequence-1 `task.created` event and `graph.task.requested` outbox record, then wakes the dispatcher; same scoped key/hash returns the original task and mismatched reuse returns 409. | api: `POST /api/v1/tasks`; authenticated principal; task repository | repository transaction: task, initial event, outbox; dispatcher wake-up | complete/current/v1 | Idempotency scope is actor + device + key; payload hash includes input and negotiated contract; dispatcher failure cannot lose durable outbox intent. PostgreSQL adapter remains tracked by 140001–140005. |
| 110004 | `python-fastapi-get-task-mandatory-p0-complete-current-v1` | `getTask` | Loads the current task projection and returns it only when both authenticated actor and device match ownership.<br>Returns status, plan revision/status, pending approval metadata and result summary; malformed, unknown and unauthorized IDs share one 404 response. | api: `GET /api/v1/tasks/{task_id}`; authenticated principal; task projection repository | api: minimal task snapshot with opaque artifact references | complete/current/v1 | Omits task input, owner/device IDs, idempotency/hash data and artifact bodies/locations. Every artifact reference explicitly requires separate authorization. PostgreSQL projection storage remains tracked by data-layer IDs. |
| 110005 | `python-fastapi-list-task-events-mandatory-p0-complete-current-v1` | `listTaskEvents` | Returns an actor/device-authorized, strictly sequence-ordered event page after an exclusive non-negative cursor.<br>Uses a bounded 1–100 page size, reports `has_more`, and sets `next_cursor` to the last returned sequence or preserves the supplied cursor for an empty page. | api: `GET /api/v1/tasks/{task_id}/events`; authenticated principal; task projection/events repository | api: stable task event page for durable replay | complete/current/v1 | Malformed, unknown and unauthorized tasks share one 404; reads are non-mutating and task-isolated. This is the durable recovery path for missed WebSocket frames. |
| 110006 | `python-fastapi-cancel-task-mandatory-p0-complete-current-v1` | `cancelTask` | Atomically records one cancellation intent, appends `task.cancellation_requested`, creates graph-control outbox intent and wakes control processing without claiming cancellation completed.<br>Repeated requests return the existing intent; terminal tasks remain unchanged. | api: `POST /api/v1/tasks/{task_id}/cancel`; authenticated principal; task projection/events | repository: updated projection, cancellation event/outbox; graph wake-up | complete/current/v1 | Actor/device authorization is enumeration-safe; dispatcher or live-publish failure cannot erase durable intent. |
| 110007 | `python-fastapi-decide-approval-mandatory-p0-complete-current-v1` | `decideApproval` | Atomically consumes one actor/device-owned pending approval only when unexpired and the submitted SHA-256 action digest matches.<br>Persists approve/deny decision, event and graph resume outbox before best-effort wake-up. | api: `POST /api/v1/approvals/{approval_id}/decision`; authenticated principal; pending approval | repository: consumed approval, projection, event/outbox; graph wake-up | complete/current/v1 | Unknown/unauthorized is 404; expired, consumed or digest mismatch is 409; action payload is never returned. |
| 110008 | `python-fastapi-open-websocket-mandatory-p0-complete-current-v1` | `openWebSocketSession` | Authenticates the desktop session and request nonce before accepting, negotiates protocol/contract version, sends a bounded welcome frame and manages live connection/subscription lifecycle.<br>Enforces connection, subscription, frame-size and per-minute message limits with deterministic close/error codes. | websocket handshake headers; device/session authentication; protocol version | memory: live connection registry, bounded broker queue and subscription state | complete/current/v1 | Loopback provides no trust; disconnect/cancellation cleans child tasks, broker registration and connection registry. |
| 110009 | `python-fastapi-stream-task-events-mandatory-p0-complete-current-v1` | `streamTaskEvents` | Replays authorized durable event pages in sequence on subscription, then delivers bounded live broker events at least once.<br>Ignores duplicates and emits `resync.required` on sequence gaps or buffer overflow so clients recover through 110005. | durable event pages; live broker; subscription cursors | websocket: `task.event`, `task.subscribed`, `resync.required` | complete/current/v1 | Durable store remains authoritative; client deduplicates event IDs; live publish failure falls back to paginated recovery. |
| 110010 | `python-fastapi-report-health-mandatory-p0-complete-current-v1` | `reportHealth` | Exposes unauthenticated secret-safe `/health/live` and `/health/ready` probes.<br>Liveness never waits for dependencies; readiness rechecks every managed dependency under a timeout, returns 503 and marks runtime acceptance false when degraded, then recovers on a later healthy probe. | process runtime; bounded telemetry/database/checkpointer/graph/provider/worker health checks | api: liveness/readiness responses; memory: content-free health counters and refreshed readiness | complete/current/v1 | Exceptions/timeouts become safe codes; no hosts, settings fingerprint, credentials or exception text are returned. Provider outage does not kill process liveness. |
| 110011 | `python-fastapi-map-domain-errors-mandatory-p0-complete-current-v1` | `mapDomainErrors` | Converts typed domain, validation, HTTP and WebSocket failures through one allowlisted registry into stable codes, static messages, safe correlation IDs, explicit retryability and bounded details. | domain exception/result; request correlation header; validation issues | api: safe error envelope/header; ws: safe error frame | complete/current/v1 | Production handlers redact exception text, stack traces, rejected values and unsafe details; verified by 14 focused mapper tests plus full backend regression. |

## Change rule

Update this map before changing a route, request/response model, authentication behavior, lifecycle resource, task transition, or socket frame.

# Versioning Log — 110009 Stream Task Events v1

Version `v1`, lifecycle `current`. Added `task.event`, subscription confirmation and `resync.required` streaming behavior with at-least-once semantics. No package, migration, graph or prompt version changed.
